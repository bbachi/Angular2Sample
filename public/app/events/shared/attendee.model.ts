
export interface IPerson {

    firstName: string;
    lastName: string;
    imageUrl: string;
}

export class Attendee implements IPerson{

    firstName: string;
    lastName: string;
    imageUrl: string;
}